<!DOCTYPE html>
<html>
  <head>
    <title>Doca</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js" type="text/javascript"></script>
    <%= favicon_link_tag 'icone.png', type: 'image/png' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'buscaCurso', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  </head>

  <body>
    <header>
      <nav  id="navbar" class="navbar fixed-top navbar-dark nav-color">
        <% if !current_usuario %>
          <%= link_to root_path, :class => 'link-path navbar-brand' do %>
            <%= image_tag("logo-color-default.png",
              :title => "Central de requisição de documentos acadêmicos", :height => 40) %>
          <% end %>
        <% else %>
          <%= link_to (current_usuario.tipo == "registro" ? registroacademico_path : biblioteca_path), :class => 'link-path navbar-brand' do %>
            <%= image_tag("logo-color-default.png",
              :title => "Central de requisição de documentos acadêmicos", :height => 40) %>
          <% end %>
        <% end %>
      </nav>
    </header>

    <div class="container-fluid" style="margin-top: 50px;">
      <% flash.each do |key, msg| %>
        <%= content_tag :div, :style => "text-align: center;", :class => "alert alert-#{ key == 'error' ? "danger" : "success" } alert-dismissable" do %>
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <%= msg %>
        <% end %>
      <% end %>
    </div>

    <section class="container-fluid" id="main">
      <%= yield %>
    </section>
    <footer class="text-muted special-color-dark card-footer footer small ">
      <div class="container text-center">
        <span>Trabalho de Conclusão de Curso.</span>
        <div>
          © 2018 Copyright: Bianca Sóta Quintãn, David da Silva Azeredo e Leandro Vicente Silva
        </div>
      </div>
    </footer>
  </body>
</html>
